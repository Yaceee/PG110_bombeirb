
/*deposer_bombe

int main(int argc, char const *argv[])
{
	
	return 0;
}
*/









/*#include <stdlib.h>
unsigned char * write_map(char * name_map)
{
	FILE * fp;

	fp = fopen(name_map, "r");
	if (fp==NULL)
	{
		return 1;
	}
	int compteur=0;
	char c;
	for (int i=0; i<3;i++)
	{
		c=fgetc(fp);
		c=fgetc(fp);
		compteur+=1;
		if (compteur==1)
		{
			int MAP_WIDTH =  atoi(c);
		}
		if (compteur==1)
		{
			int MAP_HEIGHT =  atoi(c);
		}

		c=fgetc(fp);
	}
	return 0;
}








int main(int argc, char const *argv[])
{
	write_map(name_map)
	return 0;
}
*/











